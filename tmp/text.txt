exten => _9996,1,NoOp(//////////----------Context: Call to Peers ----------//////////)
same => n,AGI(/var/lib/asterisk/agi-bin/snep/resolv_pickup_group.php,${EXTEN})
same => n,NoOp(Grupo de Captura: ${PICKUPGROUP})
same => n,Set(__PICKUPMARK=${PICKUPGROUP})
same => n,Macro(dialCallPeers,9996)
same => n,Macro(dialCallPeers,9998)
same => n,Macro(dialCallPeers,9999)
same => n,Playback(${audioNotAnswered})
same => n,Hangup()
